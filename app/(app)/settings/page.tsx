import { ComingSoon } from "@/components/shared/ComingSoon";

export const metadata = { title: "Settings" };

export default function SettingsPage() {
  return <ComingSoon title="Settings" accent="var(--fifa-blue)" />;
}
